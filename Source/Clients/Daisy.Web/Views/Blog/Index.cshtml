@model Daisy.Web.Models.PagedListBlogViewModel
@{
    ViewBag.Title = "Blogs";
}

<div class="container">
    <div class="row">
        <div class="box">
            <div class="col-lg-12">
                <hr>
                <h2 class="intro-text text-center">
                    Company
                    <strong>blog</strong>
                </h2>
                <hr>
            </div>

            @if (Model.Blogs.TotalCount > 0)
            {
                foreach (var blog in Model.Blogs.Items)
                {
                    <div class="col-lg-12 text-center">
                        <img class="img-responsive img-border img-full" src="img/slide-1.jpg" alt="">
                        <h2>
                            @blog.Title
                            <br>
                            <small>@blog.CreatedDate.ToString("MMMM dd, yyyy")</small>
                        </h2>
                        <p>@blog.Content</p>
                        <a href="#" class="btn btn-default btn-lg">Read More</a>
                        <hr>
                    </div>
                }

                <div class="col-lg-12 text-center">
                    <ul class="pager">
                        <li class="previous">
                            <a href="#">&larr; Older</a>
                        </li>
                        <li class="next">
                            <a href="#">Newer &rarr;</a>
                        </li>
                    </ul>
                </div>
            }                       
        </div>
    </div>
</div>
<!-- /.container -->