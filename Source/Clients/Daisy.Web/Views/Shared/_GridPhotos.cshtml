@model Daisy.Web.Models.PhotosViewModel

<div>
    @foreach (var photo in Model.Photos)        
    {
        <div class="col-md-6">
            <img class="photo-grid-resize" src="@photo.LargeUrl" />
        </div>        
    }
</div>